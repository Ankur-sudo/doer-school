export const numberToUSD = new Intl.NumberFormat("en-US", {
  style: "currency",
  currency: "USD",
});

export const numberToBDT_Bn = new Intl.NumberFormat("bn-BD", {
  style: "currency",
  currency: "BDT",
});
export const numberToBDT = new Intl.NumberFormat("en-BD", {
  style: "currency",
  currency: "BDT",
});
